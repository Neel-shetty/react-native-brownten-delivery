import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfileScreen = () => {
  return (
    <View>
      <Text>ProfileScreen</Text>
    </View>
  );
};

export default { name: "ProfileScreen", component: ProfileScreen };

const styles = StyleSheet.create({});
