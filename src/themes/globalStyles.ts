import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  centerRoot: {
    alignItems: "center",
    justifyContent: "center",
    flex: 1,
  },
  alignCenter: {
    alignItems: "center",
    justifyContent: "center",
  },
});
